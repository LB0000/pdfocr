{
  "name": "pdfocr-frontend",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev -p 3000",
    "dev:backend": "cd .. && cd .. && ts-node-dev --transpile-only src/backend/index.ts",
    "build": "next build",
    "start": "next start",
    "lint": "next lint"
  }
}
